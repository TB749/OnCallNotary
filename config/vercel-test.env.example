# Server-side configuration for the Vercel TEST booking backend only.
# Apply these to the backend receiving /api/booking, not as VITE_* variables.
# If Vercel proxies to a separate test Worker, set these on that Worker.
BOOKING_OWNER_EMAIL=epict5036@gmail.com
# Supply test-environment secrets privately; never commit populated values.
RESEND_API_KEY=
BOOKING_FROM_EMAIL=
BOOKING_SECRET=
ADMIN_TOKEN=
ETRANSFER_EMAIL=
PUBLIC_ORIGIN=
# ETRANSFER_EMAIL is deliberately separate: notification recipient is not a payment recipient.
