# P1 Calendly migration checkpoint

## Current decision and implementation

## Separate executable test runner work in progress

Final rebuild completed after live selector/security handling fixes. Only a non-live TC001 smoke test was run afterward (173840); no further live submission attempts. Live matrix is blocked by Calendly security, not by user email authorization. Next action: user/host resolves Calendly support code below; do not rerun live submissions until resolved. Generated live report is a draft because Word layout verification remains declined.

LATEST STOP POINT: first final-submit attempt in P1_UI_Test_20260908-173419 reached the real invitee form, verified name/email/a1 prefill and selected September 9 at 9 AM Toronto. After Schedule Event, Calendly explicitly displayed “This booking cannot be completed” for security reasons. Support code tid-rb-686479e8-216f-4f07-b155-de6061c776e2. No booking confirmation or email receipt verified. STOP all automated live submission attempts; do not bypass the restriction or retry the same case until resolved with Calendly. Screenshot TC001-failure.png and results-reviewed.json record the rejection. P1_UI_Test_Live_20260908.docx is the corrected one-case live draft. Earlier 64-case UI report remains separate and uses fixtures.

Runner fixes now cover embedded Select a Day heading, back-to-day navigation, accessible Next TIME button, name/email labels and SECURITY BLOCKED immediate stop. Rebuilding final EXE. Source ui-test-runner/runner.py; root and source configs both use tombee10@gmail.com. Exact next action: finish final build and non-live smoke only, then report the Calendly security blocker. No live continuation without resolving it. Word rendering was declined, so generated DOCX layout is still unverified.

Live run continuation: customer email confirmed and configured as tombee10@gmail.com. First two live attempts submitted ZERO bookings; narrow Calendly iframe uses “Select a Day” and a separate time screen, requiring different selectors/back navigation. Evidence in P1_UI_Test_20260908-171126 and P1_UI_Test_20260908-171803. Correcting runner selectors and rebuilding before first successful live case. Test report rendering remains unapproved; do not bypass the declined Word action.

User confirmed yes: use tombee10@gmail.com as the customer invitee email for live test bookings. Both runner configuration files updated. Live test bookings are authorized through the designated tombee10 event. This does not approve Word rendering previously declined.

User requested a separate Windows EXE to test every service, operation, eligible mode/platform and before/from 6 PM, save screenshots in a Word report under D:\P1. User then explicitly authorized actual Calendly test bookings and notification delivery to tombee10@gmail.com. User confirmed tombee10@gmail.com as the customer invitee email; real test submissions are authorized. Owner notification recipient remains a Calendly setting.

Created ui-test-runner/runner.py, build.ps1, README.md, report-template.docx and P1_UI_Test_Config.json; root executable P1_UI_Test_Runner.exe and root config. No website source or private settings changed in this task. Dependencies installed into ignored qa/runner-build-packages only. EXE bundles Python, Playwright and DOCX libraries; requires Microsoft Edge and running local P1. Live mode records confirmations, journals before final click, stops on uncertain submission, never cancels or retries such bookings. Live browser flow is NOT verified yet. Configuration now defaults to live with customerEmail:tombee10@gmail.com.

Full UI run completed: D:\P1\P1_UI_Test_20260908-165213.docx with 119 screenshot placements from 64 cases (55 PASS UI, 9 PASS BLOCKED). Results and original PNGs are in matching D:\P1\P1_UI_Test_20260908-165213 folder. Uses fixture Calendly only, no real submissions/emails. The selected Experiment Analysis template supplies the report styles/page system. Packaged render_docx.py failed because LibreOffice is absent. Word rendered the source template successfully under escalation; the subsequent Word render request for generated report was rejected by user. Final report visual layout is therefore UNVERIFIED and must not be represented as QA-complete. Earlier smoke and interrupted run artifacts are timestamped separately.

Latest build includes readiness check, footer title replacement and preserved document template. Final packaged EXE smoke test passed TC001 in UI mode and generated P1_UI_Test_20260908-170918.docx. Local Vite restarted on port 5173 (session 77849). Exact next action: run TC001 with corrected live selectors, then continue only after checking its submission result. Report-render approval was declined and must not be bypassed. Main 64-case report remains the 165213 document, not the one-case smoke report.

Latest user edit: removed the six duplicate service shortcuts above Smart Booking from src/NotarySite.tsx. Keep the form's service dropdown, main service cards and their preselection links. The standalone Book Mediation button remains commented out by the user.

D:\P1 on codex/p1 is the working folder. Preserve all uncommitted work and use the client's production baseline, never demo code. Reply in English. The user said to finish code first, and explicitly instructed every valid Smart Booking request to use https://calendly.com/tombee10/notary-commissioner-standard-1 for now.

Smart Booking uses unchanged shared/catalog.mjs estimates and validation, then the shared Calendly event. Names/emails and requested service details prefill the verified a1 text question. The UI explains that the shared 30-minute event does not establish requested service, format, duration or final price. Custom scopes show known charges only. Original calendar, contact settings, production appearance and six service cards remain.

## Completed code cleanup

Removed custom booking quote signing, submission/status handling, confirmation/reservations and payment-receipt actions from server/api.mjs. Retired URLs return 410 without mutation. Business inquiries keep their existing transactional storage, idempotency, rate limiting and mail delivery. Email retries only process inquiry jobs, preventing legacy booking mail replay. Owner.tsx provides protected inquiry/saved-record viewing and inquiry mail retry; legacy bookings are read-only.

Preserved database schema, migrations, .local data, private environment files and all saved records. Kept the Worker/local API because BusinessInquiry still needs them. Cleaned .env.example to retained inquiry settings. Replaced obsolete booking browser tests with inquiry/owner coverage and retained catalogue tests. README.md and CALENDLY_SETUP.md now describe the actual implementation.

Changed during cleanup: server/api.mjs, src/Owner.tsx, .env.example, tests/booking.test.mjs, tests/browser-check.mjs, README.md, CALENDLY_SETUP.md, AGENTS.md and this handoff. Earlier migration changes remain in src/SmartBooking.tsx, src/CalendlyBooking.tsx, src/NotarySite.tsx, src/booking.css, shared/calendly.mjs and Calendly tests. The pre-existing deletion of config/vercel-test.env.example is preserved. shared/catalog.mjs is unchanged.

## Verification

Latest local fix: service shortcuts pointed to outer #book, leaving users above the actual form. Updated active service-specific links (shortcut grid, pricing cards, service cards and footer) in src/NotarySite.tsx to #smart-booking while retaining preselection. Preserved the user's commented-out Book Mediation button. Headless Edge checks passed at 1440px and 390px for shortcut/card selection and scrolling; TypeScript passed. No deployment. Next action: user retries the shortcut in local preview.

User manual test case 1 passed: user reports receiving a Calendly booking email. This verifies user-reported delivery for that booking, not service-detail/estimate prefill. User requested the local site again for further manual testing; http://127.0.0.1:5173/ responds HTTP 200. No automated booking or email submitted.

25/25 tests pass, including inquiry transaction rollback, idempotency, protected access, retired endpoint non-mutation and exclusion of legacy email jobs. TypeScript and production build pass. Business inquiry/owner browser checks passed using isolated DB and fake mail. Calendly browser regression is being finalized. Frontend preview is at http://127.0.0.1:5173/.

Earlier real Calendly inspection verified the event loads, is 30 minutes, has an a1 text question and no payment. No platform is configured; timezone is New York; future dates are enabled; seal capacity is unknown. September showed no selectable dates. These do not block the user-authorized shared test mode. Actual invitee-form prefill, email delivery and production settings remain unverified. User-reported test notification recipient: tombee10@gmail.com. Do not change production or inquiry recipients to this address.

## Release boundary and exact next action

Latest repository change: user requested origin switch to https://github.com/TB749/OnCallNotary.git. Updated local Git configuration and recorded the new development destination in AGENTS.md and README.md. Prior dry-run push succeeded; no actual push or branch creation occurred. Vercel linkage for the new repository is unverified. All working changes remain intact.

No live booking, email, push or deployment. Previous developer main deployment was 99458b7 at https://notary2026.vercel.app/. Current local work is uncommitted on codex/p1. Do not publish until requested; compare remote changes before any push. Client repository and Cloudflare remain untouched.

Final verification: Calendly browser regression also passed; git diff --check passed. Both browser suites used isolated fixtures and sent no live mail. Local code work is complete for the shared-event migration and booking cleanup.

Latest local review: user requested local testing first. Existing frontend at http://127.0.0.1:5173/ returned HTTP 200; reused it without replacing its process. Requested browser opening in Codex (queued). Both tests/browser-check.mjs and tests/calendly-browser.mjs passed again using isolated fixtures, with no live bookings or emails. No code changes or deployment during this review.

Exact next action: user reviews the local website; address their findings. Browser-tested inquiry submission used a fixture API, so this does not claim the ordinary frontend preview has a configured inquiry backend. Calendly settings and live booking/email verification remain deferred.

## September 10 resume verification
Read all five project Markdown files and inspected Git status, site diff, runner security-stop code and saved final results. Branch remains codex/p1; origin is TB749/OnCallNotary. Final EXE and TC001 fixture smoke are complete; results-reviewed.json confirms SECURITY BLOCKED for the live attempt. Older next-action paragraphs above are superseded by this checkpoint. No live retries, report rendering, push or deployment performed. Vite reported ready at http://127.0.0.1:5173/, but the subsequent HTTP check timed out; preview response and inquiry backend readiness are not verified. Website source unchanged. Exact next action: obtain confirmation that Calendly has resolved support code tid-rb-686479e8-216f-4f07-b155-de6061c776e2 before resuming automated live tests. Report layout remains unverified following the earlier declined render.

## September 10 cost matrix workbook
Created outputs/01a08dbe-6d1e-7351-a65b-19934e3668c3/On_Call_Notary_Cost_Matrix.xlsx from the discussed service/mode/period matrix. Numeric prices, travel labels, proposed affidavit in-person fee and unresolved after-hours pricing preserved. Value/error checks and rendered visual review passed. Website pricing unchanged. User requested affidavit commissioning before 6 PM in person at 29 + 10; fee frequency and extension to other service categories remain unanswered. C: reported full; artifact creation used D:. Exact next action: user reviews the Excel matrix and confirms the scope of the proposed 10-dollar fee before implementation.

## September 10 updated Excel matrix implemented
User explicitly requested applying D:/Esther Master/On_Call_Notary_Cost_Matrix.xlsx. Read all 72 table cells and number formats; table edits supersede stale notes. Shared catalogue now adds one 1000-cent in-person commissioning fee before 6 PM for Affidavits, Business and International, keeps additional seals at 1500 cents, removes regular online mediation, and makes urgent online drafting regular-priced and calculated. Mobile travel and urgent mediation remain review-required. Updated form help, pricing-card note and FAQ while preserving user-commented legacy calendar. Added tests/cost-matrix.test.mjs with workbook-derived expectations for every cell, multi-seal and 6 PM checks. Extended Calendly browser tests for the changed UI and replaced the removed legacy-frame dependency with an independent frame for message-source rejection. Updated ui-test-runner/runner.py and rebuilt P1_UI_Test_Runner.exe; runner now has 62 cases. Historical reports unchanged. Updated AGENTS.md, README.md and runner README. Tests: 28/28 unit tests, TypeScript, production build, Calendly fixture browser suite and git diff --check passed; local preview HTTP 200. No live bookings, emails, push or deployment. Exact next action: user manually checks the updated prices at http://127.0.0.1:5173/. Automated live submission remains blocked pending Calendly security resolution.
Rebuilt EXE startup: default temporary extraction failed while C: was full; --help passed with TEMP and TMP set to D:/P1/qa/runner-temp. Users need available temporary disk space to launch the EXE. No new UI report or live run was generated.

## September 10 GitHub source packaging
User authorized packaging minimum code and pushing to TB749/OnCallNotary. Remote advertised no refs. Keep codex/p1 and existing history; no main or production deployment. Exclude outputs, standalone QA runner/config, binaries, reports, dependencies, private environment files and local data. Removed unused root screenshot from Git index only; local file preserved. Include website assets, shared pricing/Calendly code, retained inquiry backend/schema, sanitized templates, regression tests and project documentation. Previous 28 unit tests, TypeScript, build and Calendly browser suite passed. Next action: finish staged-package verification, commit, create source ZIP and push codex/p1; verify remote commit.
